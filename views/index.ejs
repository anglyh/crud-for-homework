<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD de Usuarios</title>
  <link href="/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-6">
  <h1 class="text-3xl font-bold mb-4">Usuarios</h1>
  <form action="/users" method="POST" class="bg-white p-6 rounded shadow-md mb-6">
    <div class="mb-4">
      <label for="name" class="block text-gray-700 font-medium mb-2">Nombre:</label>
      <input type="text" id="name" name="name" required class="border border-gray-300 rounded p-2 w-full">
    </div>
    <div class="mb-4">
      <label for="email" class="block text-gray-700 font-medium mb-2">Email:</label>
      <input type="email" id="email" name="email" required class="border border-gray-300 rounded p-2 w-full">
    </div>
    <div class="mb-4">
      <label for="password" class="block text-gray-700 font-medium mb-2">Contraseña:</label>
      <input type="password" id="password" name="password" required class="border border-gray-300 rounded p-2 w-full">
    </div>
    <button type="submit" class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600">Crear Usuario</button>
  </form>
  <hr class="my-6">
  <h2 class="text-2xl font-semibold mb-4">Lista de Usuarios</h2>
  <ul class="bg-white rounded shadow-md p-4">
    <% users.forEach((user) => { %>
      <li class="flex justify-between items-center border-b border-gray-300 py-2">
        <span class="text-gray-800"><%= user.name %> (<%= user.email %>)</span>
        <div>
          <a href="/users/update/<%= user._id %>" class="text-blue-500 hover:text-blue-700 mr-2">Editar</a>
          <a href="/users/delete/<%= user._id %>" class="text-red-500 hover:text-red-700">Eliminar</a>
        </div>
      </li>
    <% }) %>
  </ul>
</body>
</html>
